{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the ordi web layer may ask the desktop shell to do: OS notifications, the taskbar badge, native events (quick-add shortcut, deep links), opening the browser for sign-in, and applying an update.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-badge-count",
    "notification:default",
    "updater:default",
    "process:allow-restart",
    "deep-link:default",
    "opener:allow-open-url"
  ]
}
