/**
 * In-app tab system (Linear-style): open tasks/projects/pages in tabs.
 * Placeholder scaffold — full implementation lands with the tabs feature.
 * Pages call usePageTitle(title) to name the active tab.
 */
export function usePageTitle(title?: string | null): void {
  void title;
}
