/**
 * Per-project access management: visibility (workspace/private) + members
 * with roles. Embedded in the project settings tab.
 * Placeholder scaffold — full implementation lands with the access-control feature.
 */
export function ProjectAccessPanel({ projectId, canManage }: { projectId: string; canManage: boolean }) {
  void projectId; void canManage;
  return null;
}
